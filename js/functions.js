    const submitForm = () => {
        let name = document.getElementById("name").value;
        let sms = document.getElementById("text").value;
        console.log(" Name : " + name + " \n Email : " + sms);
    };